{
  "name": "nuro",
  "version": "0.1.1",
  "description": "JavaScrict framework",
  "author": {
    "name": "John Egan"
  },
  "license": "MIT",
  "devDependencies": {
    "esm": "^3.2.25",
    "jest": "^24.9.0",
    "prettier": "1.19.1",
    "rollup": "^1.27.13"
  },
  "scripts": {
    "format": "prettier --print-width 100 --no-semi false --single-quote true --write \"./**/*.{js,json}\"",
    "build": "rollup -c",
    "watch": "rollup -c -w",
    "test": "npm run build && jest --coverage"
  }
}
